# CLAUDE.md

See `AGENTS.md`, then read `docs/` in the order `docs/README.md` gives. Start from the first phase
in `docs/PHASES.md` whose exit criterion is not met.

The only commands are the ones in `package.json`. Do not invent others.
